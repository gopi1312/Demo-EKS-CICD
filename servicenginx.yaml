apiVersion: v1
kind: Service
metadata:
  name: "service-nginx-albdemo"
  namespace: "alb-demo"
spec:
  ports:
    - port: 81
      targetPort: 81
      protocol: TCP
  type: NodePort
  selector:
    app: "nginx-albdemo"
